#include<stdio.h>
#include<math.h>
float main()
{
float a,n;
printf("key in the value for which sqrt is to b found");
scanf("%f",&n);
a=sqrt(n);
printf("the sqrt of a given number n=%f is %f\n",n,a);
}
